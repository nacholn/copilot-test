# ============================================
# BACKEND API CONFIGURATION
# ============================================

# Database Configuration (PostgreSQL via Docker)
# These should match your docker-compose.yml settings
DB_HOST=localhost
DB_PORT=5432
DB_NAME=cycling_network
DB_USER=cycling_user
DB_PASSWORD=cycling_password

# JWT Secret (Change in production!)
# Generate a secure random string for production
JWT_SECRET=your-secret-key-change-in-production

# ============================================
# WEB APP CONFIGURATION
# ============================================

# API URL for web app (Next.js frontend)
NEXT_PUBLIC_API_URL=http://localhost:3001

# Web app URL
NEXT_PUBLIC_WEB_URL=http://localhost:3000

# ============================================
# MOBILE APP CONFIGURATION (Expo)
# ============================================

# API URL for mobile app
# Note: Use your computer's IP address when testing on physical devices
# Example: EXPO_PUBLIC_API_URL=http://192.168.1.100:3001
EXPO_PUBLIC_API_URL=http://localhost:3001

# ============================================
# IMPORTANT NOTES
# ============================================
# 
# ❌ SUPABASE IS NOT USED IN THIS PROJECT
# This project uses Docker PostgreSQL with JWT authentication
# Do NOT add SUPABASE_URL or SUPABASE_ANON_KEY variables
#
# ✅ To get started:
# 1. Copy this file to .env (cp .env.example .env)
# 2. Start Docker: docker-compose up -d
# 3. Install dependencies: npm install
# 4. Start backend: cd apps/backend && npm run dev
# 5. Start web: cd apps/web && npm run dev
#
# For detailed setup instructions, see DOCKER_SETUP.md
